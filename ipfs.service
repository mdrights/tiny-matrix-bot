[Unit]
Description=IPFS Daemon
After=syslog.target network.target remote-fs.target nss-lookup.target

[Service]
Type=simple
ExecStart=/home/user/go-ipfs/ipfs daemon --enable-namesys-pubsub
User=user

[Install]
WantedBy=multi-user.target
